import keyMirror from 'key-mirror';

const types = keyMirror({
  FLUSH: null,
  INITIALIZE: null,
  LOGIN_EMAIL: null,
  SET_USER: null,
  TOGGLE_NAV: null,
  SET_NAV: null,
});

export default types;
